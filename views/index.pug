extends layout

block content
  h1= title
  p Welcome to Omaha #{title}
  button(type="button", class="btn btn-primary", onclick="update()") Press Me
  br
  br
  div(id = "target") 
    b Display
    a(href='/shopperstop') shopperstop page link
  script.
   var client_count=0;

     function update()
      {
       client_count = client_count+1;
       document.getElementById("target").innerHTML=`Value now ${client_count}`;
     }